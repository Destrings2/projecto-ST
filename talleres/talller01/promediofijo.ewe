main:
  readInt(times)
  i := 1
  suma := 0
  one := 1

loop:
  if i > times then goto end
    readInt(temp)
    suma := suma + temp
    i := i + one 
    goto loop

end:
  temp := suma / times
  writeInt(temp)
  halt

equ times M[0]
equ i M[1]
equ suma M[2]
equ one M[3]
equ temp M[4]
