main:
  i := 0
  suma := 0
  one := 1
  minus := 0

loop:
  readInt(temp)
  if temp < minus then goto end
    suma := suma + temp
    i := i + one
    goto loop

end:
  temp := suma / i
  writeInt(temp)
  halt

equ times M[0]
equ i M[1]
equ suma M[2]
equ one M[3]
equ temp M[4]
equ minus M[5]
