a := 0
b := 0
c := 0
r := 0
main:
     readInt(a)
     readInt(b)
     readInt(c)
     if a < b then goto else
	r := a
	goto cond
     else:
        r := b
     cond:
     if r > c then goto else1
     	r := c
     else1:
     writeInt(r)
end:
     halt
equ a M[0]
equ b M[1]
equ c M[2]
equ r M[3]